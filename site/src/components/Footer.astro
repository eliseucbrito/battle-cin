<footer class="footer">
  <hr class="section-divider" />
  <div class="container footer-inner">
    <div class="footer-logo">
      Battle<span class="footer-accent">CIn</span>
    </div>
    <p class="footer-text">
      Desenvolvido para a disciplina <strong>Estruturas de Dados e
      Orientação a Objetos (EDOO)</strong> — Centro de Informática (CIn),
      Universidade Federal de Pernambuco (UFPE).
    </p>
    <p class="footer-members">
      ELISEU CORDEIRO • MATHEUS VICTOR • CLEYTON CARDOSO • HUGO JOSÉ
    </p>
    <p class="footer-year">2025</p>
  </div>
</footer>

<style>
  .footer {
    padding: 48px 0;
    background: var(--elevated);
  }

  .footer-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 12px;
  }

  .footer-logo {
    font-family: var(--font-display);
    font-size: 28px;
    color: var(--pure-white);
    letter-spacing: 2px;
    margin-bottom: 8px;
  }

  .footer-accent {
    color: var(--accent);
  }

  .footer-text {
    font-family: var(--font-serif);
    font-size: 14px;
    color: var(--ink-secondary);
    max-width: 500px;
    line-height: 1.6;
  }

  .footer-text strong {
    color: var(--ink);
    font-weight: 600;
  }

  .footer-members {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 1px;
    color: var(--ink-muted);
  }

  .footer-year {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.5px;
    color: var(--ink-muted);
  }
</style>
